# UMBRA-AS-004 — bounded continuation action selection and integrated viability

The exact directive is retained in the user-provided task packet. This local
spec records the operative lock: extend the qualified hypothetical substrate
with bounded multi-step AND/OR continuation semantics; build a common-root
source-backed continuation set before candidate evaluation; classify candidate
effects as `PRESERVED`, `DESTROYED`, or `UNKNOWN`; use strict preservation
relations only for candidate elimination; then retain the existing distributed
competition and candidate-local stochastic resolution for residual nondominance.

The planner is non-executable, non-learning, bounded, deterministic, and
recomputed from the current root. Only the current selected candidate reaches
Governance, Embodiment, execution, VerifiedOutcome, and selected-only learning.

Scientific execution is locked only after pure tests, regression checks,
observer-only parity, provenance checks, boundedness checks, Authority 3.0,
Governance, and `git diff --check` pass. A post-lock scientific failure is
terminal evidence; no retry or reseed follows.
