# UMBRA-AS-003P-R6B-R1 — route-control continuity repair

R6B remains terminal with `AS003PR6B_IMPLEMENTATION_FAIL`. This fresh generation
repairs only the WorldModel-owned verified route-evidence lifecycle.

## Locked scope

- Exact same-target verified `ORIENT` may preserve an active opportunity/body-bound
  route episode and is recorded as a non-translational route-control step.
- Verified `APPROACH` remains the only translational movement count.
- Route-control order, completion lag, and verified provenance are retained.
- Exact opportunity identity and body-schema equality are required; ambiguity,
  target change, body change, unrelated action, denial, or unverified outcome fails
  closed.
- Route learning remains default-off, write-only, and has no planner, modal/L2,
  candidate, arbitration, or action-selection reader.
- The R6B assay fixture remains fixed: seed `6103`, scenario `S0`, max `8` ticks,
  one nominal leg and one existing-fault failure leg, with retries/reseeds `0/0`.

## Evidence boundary

No organism creation or tick is permitted before a committed execution protocol
lock. After the lock, exactly one nominal and one failure assay are authorized.
The historical R6B result and evidence remain immutable.
