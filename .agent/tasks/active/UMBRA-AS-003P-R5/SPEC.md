# UMBRA-AS-003P-R5

## Authority

Fresh non-formal common-root observer qualification from exact baseline
`3b0694ed5a6cddbf7c22c4f419a01e21e20c0e6c` after Architect acceptance of
`REPO001_EMPLOYER_GRADE_FRONT_DOOR_QUALIFIED`.

## Objective

Prepare one R0/S0 seed `45878900` organism root, freeze its durable organism,
Habitat, and RNG state, clone it into independent CONTROL and SHADOW branches,
prove exact premeasurement semantic parity, then execute one 500-tick measured
leg per branch. Observer parity is evaluated before any modal evidence is
interpreted.

## Scope locks

- Production and existing scientific semantics are immutable.
- One prepared root birth is authorized; the root executes zero measured ticks.
- CONTROL and SHADOW each load once and execute exactly 500 measured ticks.
- The comparator and execution protocol freeze before measured execution.
- Retries and reseeds are zero.
- Modal output remains write-only shadow evidence and has no action authority.
- Historical R1/R3 modal traces remain invalid and cannot tune R5.
- AS-004, CLOSE-03, and automatic successor work are prohibited.

## Terminal boundary

Return exactly one R5 verdict and stop at the first mandated terminal condition.

