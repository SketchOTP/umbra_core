# Handoff

AS-003P-R1 remains permanently terminal `AS003PR1_OBSERVER_EFFECT_FAIL`.
AS-003P-R2 is the active zero-run forensic authority. It must validate the
frozen comparator independently, then attribute the retained pair without
changing production or rerunning the organism.
