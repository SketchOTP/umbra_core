# Handoff

AS-003S is active. The next engineer must preserve the exact baseline and locked
contracts, make no live owner mutation before the replacement transaction commits,
and stop if event plus prospective snapshot cannot share one SQLite transaction.
