# UMBRA-AS-003P-R5A

## Authority

Fresh retained-root common-root observer qualification from exact baseline
`04946e3fc977593bf41d1eb40f1fc8517ef289aa`, after Architect acceptance of
permanent R5 verdict `AS003PR5_PROTOCOL_PREFLIGHT_FAIL`.

## Objective

Adopt the already-created R5 tick-0 root as immutable scientific input, prove
the corrected raw-TEXT snapshot metadata protocol before branch load, clone it
into exactly two independent CONTROL/SHADOW stores and Habitat states, prove
zero-difference premeasurement parity, then execute one 500-tick leg per branch.
Modal evidence may be interpreted only after observer semantic parity passes.

## Scope locks

- R5A root creation count is zero; `fixture.prepare()` is forbidden.
- The R5 comparator, parity source contract, corpus, and lock remain byte-identical.
- Production and existing scientific-test semantics remain unchanged.
- The retained R5 database/Habitat are read-only archival inputs.
- CONTROL and SHADOW load once and execute exactly 500 measured ticks.
- Retries/reseeds are zero; no alternate horizon or second pair exists.
- Modal output remains evidence only and has no action-selection authority.
- AS-004, CLOSE-03, and automatic successor work are prohibited.

## Terminal boundary

Stop at the first directive-defined adoption, protocol, clone, premeasurement,
frozen-execution, observer-parity, frame, or evidence failure and return exactly
one permitted R5A verdict.
