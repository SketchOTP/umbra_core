# UMBRA-AS-003P-R5A result

## Verdict

`AS003PR5A_OBSERVER_SAFE_MODAL_EVIDENCE_NONDISCRIMINATING`

## Result boundary

R5A reused the immutable R5 tick-0 root and created no replacement root. After
synthetic metadata qualification, byte-identical comparator inheritance, two-way
clone independence, zero-difference premeasurement parity, and a committed
execution lock, exactly one CONTROL and one SHADOW branch completed 500 measured
ticks each. Retries/reseeds remained `0/0`.

The prospective comparator found zero semantic differences. Action timeline,
candidate identities, authoritative event semantics, final authoritative state,
Habitat, and RNG all matched. Modal evidence was therefore eligible for fresh
interpretation.

The shadow path produced 500 complete frames and 2,686 profiles: 2,664
`STRONG_MAY` and 22 `UNKNOWN`, with branch-frontier peak 4 and no overflow.
It produced zero candidate-pair profile distinctions. Fifty-seven decisions
directly exposed the AS-003L residual conflict class, and none were distinguished.

AS-003L disposition: `BLOCKER_NOT_EXPRESSED_DESPITE_EXPOSURE`.
AS-002 future relation: `NO_RELATION_SUPPORTED`.

Production delta is zero. Modal planning remains shadow-only and has no action
authority. No successor was started.

Authority 3.0, governance, retained-root readback, `git diff --check`, and
public-link validation pass. Closeout SHA-256 is
`37b83cf2b139f8d1a284e733507156e508ed2d5c5a9e346e3064da5809ec1a72`;
final evidence manifest SHA-256 is
`271a717821c07defbc0b5b89191065f0e5923e60bbd71dac0855fd45ecebb805`.
