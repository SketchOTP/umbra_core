# UMBRA-AS-017 — Integrated Core Closure and Qualification Readiness

Status: PRE-LOCK DEVELOPMENT; adopted 2026-09-13.

This repository-local record summarizes the Architect directive supplied in the
current task. The authoritative request is to close the existing AS-017
candidate from V5, not to create AS-018 or launch formal science.

## Subject and boundaries

- V5 subject: `fe783295a0de175b88000a9ffadfd1b7ff4afa38`.
- AS-017 working branch baseline: `f9bb1f809602b584b4931d887115d252d164e5e2`.
- Published master at adoption: `fd8c50e1134d7d2ef54e20148ac9b7880e63708d`.
- Preserve V1/V2/V3/V4C/V5 and all failed or scoped reviews.
- Preserve unrelated `.gitignore` and `AGENTS.md` changes in the original
  checkout; this isolated checkout is the execution subject.
- No formal lock, formal seed, formal organism, threshold relaxation, hidden
  Habitat input, whole-core rewrite, or automatic CLOSE-03 is authorized.

## Required closure

1. Map and independently test active prediction-to-Governance-to-adapter-to-
   verifier-to-physiology paths, including all four dimensions, timing,
   clamping, parameters, denial, body limits, heading translation, and restart
   boundaries. Repair relevant owner defects in scope.
2. Resolve the seven legacy failures and orphan import individually. Repair a
   current defect; replace stale assertions only with equal-or-stronger tests;
   keep inherited failures, skips, exclusions, collection, and execution
   visible.
3. Preflight the literal population, lifecycle, boundedness, S3, ablation,
   publication, finalization, and validator surfaces using excluded fixtures;
   do not execute formal seeds.
4. Publish one restorable candidate/readiness packet with raw outputs, hashes,
   configuration and dependency fingerprints, governance checks, and a
   goal-to-evidence gap map. Return `READY_FOR_ARCHITECT_LOCK_REVIEW` only when
   E0–E5 are supported.

Unknown is not proven and is not universal impossibility. Learned models may be
wrong; authoritative execution and physiology projection must agree with the
actual execution owner. Existing V5 evidence remains predecessor evidence and
is not reassigned to changed behavior.
