# AS-003P-R3 result

Terminal verdict: `AS003PR3_TRUE_OBSERVER_EFFECT_FAIL`.

The prospectively locked source-semantic comparator qualified `30/30` with
false positives/negatives `0/0`. The one frozen R0/S0 seed `45878900`, horizon
`500` control/shadow pair completed. Timeline, candidate semantic identities,
and RNG matched, while two source-contract-authoritative `body_instance_id`
values differed in the tick-1 event and final Embodiment-adapter state.

The parity gate therefore failed. The fresh planning trace remains durable but
was not interpreted. No modal distribution, conflict exposure, AS-003L
reassessment, AS-002 relation research, retry, reseed, repair, or successor
occurred.
