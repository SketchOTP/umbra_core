# UMBRA-AS-003P-R6C result

## Terminal verdict

`AS003PR6C_ROUTE_AFFORDANCE_PLANNING_FRAME_QUALIFIED`

R6C is a fresh, bounded, zero-organism generation from exact baseline
`979915fa0c731800212fa0d5d5b3f1d709aae636`. It qualifies an additive,
shadow-only V2 planning-evidence projection; it does not qualify a planner,
modal action authority, ordinary action selection, or integrated viability.

## Implementation

The frozen implementation adds immutable `route_experience_support` and
`affordance_support` fields to `AS003P_PLANNING_EVIDENCE_FRAME_V2`. Successful
V2 route experiences join exact opportunity identity, body schema, and terminal
capability, preserving ordered verified route-control steps, observed duration,
movement/control counts, completion lags, and provenance. Their evidence is
`VERIFIED_OBSERVED_SUPPORT` with modality `MAY`; failures remain separate and
failure-only or unmatched evidence is `UNKNOWN`. V1 records remain readable and
are never upgraded with invented control steps.

Learned `ACTIVE` `inspect` affordance evidence projects as `MAY` only when a
matching policy-visible opportunity instance exists. Authored priors, missing
instances, weakened/superseded beliefs, and unsupported joins remain `UNKNOWN`.
No Habitat truth, confidence threshold, interpolation, route-learning change,
or modal/action-selection reader was introduced.

## Evidence

The retained R6B-R1 projection produced one successful V2 witness for one exact
opportunity/body-schema pair: seven ordered route-control steps, one
translational `APPROACH`, three `ORIENT` steps, two premature terminal attempts,
observed duration seven logical ticks, and one separately retained failure
record. This is a retrospective projection of retained evidence, not a new
organism run.

The focused pure projection/regression suite passed `62/62` twice. Static scope
analysis found zero readers of the new fields outside projection/shadow capture;
legacy modal inputs and semantics remained unchanged. Boundedness,
determinism, deep immutability, V1 compatibility, affordance negative controls,
source fingerprints, and policy isolation passed.

Evidence root:
`/srv/ATLAS/100_ACTIVE/Projects/UMBRA-CORE/evidence/live-evidence/umbra-as-003p-r6c-route-affordance-frame-r1/`.

The final GitHub closeout commit and evidence-manifest digest are published in
the append-only `AS003PR6C_FINAL_COMMIT_CORRECTION.json` artifact.

## Integrity and boundary

- baseline: `979915fa0c731800212fa0d5d5b3f1d709aae636`
- governance start: `36597447b4ec0ec7b043195128e806f274641f5f`
- implementation/tooling: `c19ae6b3777927515d5cc9f223e4e6f426b8f07a`
- dedicated freeze: `71074dc8bfe6fce3e66398f92f642537686582c3`
- production semantic delta after baseline: additive frame/shadow projection only
- existing-test semantic delta: `0`; additive focused tests only
- organism/load/tick/control/shadow/diagnostic/qualification runs: `0/0/0/0/0/0/0`
- retries/reseeds: `0/0`
- Authority 3.0: `PASS`; Governance: `PASS`; `git diff --check`: `PASS`

R6C does not rewrite R6B's permanent failure and does not grant route evidence
to planning or action selection. Recommendation only:
`UMBRA-AS-003P-R7 — Prospective Route-Informed L2 Common-Root Shadow Qualification Candidate`.
No successor was started.
