# Handoff

AS-003P-R4 is in zero-run source-authority reconstruction. R3 remains terminal
and its modal trace is not interpretable. The active question is the semantic
role of `body_instance_id` and the prospectively correct observer protocol.
