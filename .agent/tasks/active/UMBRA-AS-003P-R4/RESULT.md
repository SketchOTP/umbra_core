# Result

Terminal verdict: `AS003PR4_BODY_REPLACEMENT_IDENTITY_DEFECT_CONFIRMED`.

The body attachment name is a run-local fresh relational name across independent births and exact within one persisted organism history. The retained R3 pair is consistent with one pre-shadow bijective rename, but R3 remains permanently failed. The adapter's documented true-replacement path cannot create a new body identity, and SelfModel replacement is not linked to adapter/habitat identity. No future observer pair or successor is authorized.

Validation: pure alpha-equivalence corpus `12/12 PASS`; production/existing-test delta `0`; organism/control/shadow/diagnostic runs `0`; retries/reseeds `0/0`.
