# UMBRA-AS-003P-R6F — static feasibility gate

## Current disposition

The R6F static Phase B and Phase C gates pass. This is not a terminal R6F
verdict and does not authorize an organism by itself.

Phase B found an existing ordinary policy-visible transition using the
current recoverability seam: with a policy-visible bounded resource support,
`IDLE` preserves a feasible route margin while an ordinary emitted `MOVE`
candidate remains hard-admissible and makes the projected margin infeasible.
The probe uses existing `BOUNDS`, drift, verified effect, and support fields;
it does not use Habitat coordinates or a new threshold. This is a static
source-seam feasibility result, not a future route guarantee.

Phase C established the narrow later-root applicability path: a completed
verified V2 route experience can be checked against an exact policy-visible
WorldModel entity, the same body schema, an eligible fact kind, matching
terminal capability, and preserved provenance. Missing, ambiguous, stale, or
schema-mismatched evidence fails closed. The static fixture is a pure
applicability test and is not claimed as a live common root.

The common-root provenance contract is locked in
`AS003PR6F_COMMON_ROOT_PROVENANCE_CONTRACT.json`. A live root may be admitted
only after an actually executed and verified route experience has been
durably learned before the root tick. Candidate-specific evidence, synthetic
deadlines/horizons, and hidden Habitat truth remain forbidden.

## Validation

- Pure isolated feasibility tests: `4 passed` twice; no organism/runtime tick.
- Static audit artifacts are readback-hashed in the R6F evidence root.
- Production delta: `0`.
- Existing scientific-test semantic delta: `0`.
- Organism/load/tick/control/shadow/diagnostic counts: `0/0/0/0/0/0`.
- Retries/reseeds: `0/0`.

## Evidence

Evidence root (local/internal provenance):
`/srv/ATLAS/100_ACTIVE/Projects/UMBRA-CORE/evidence/live-evidence/umbra-as-003p-r6f-prospective-common-root-option-r1/`.

Static natural-loss artifact SHA-256:
`53e2a2a9b879beb1115cba070e283abcaecee3e4633552f8060d60eae1cb3a68`.

Static route-applicability artifact SHA-256:
`f9b8c9c7698baba673e1dea25587ea1c9c603f8f739dab87724c4521a610ef59`.

Common-root contract SHA-256:
`5d5e17e50d66bc477ee5b534177a03ec4e8f1db5a72695b94e4a5402a50f55ad`.
