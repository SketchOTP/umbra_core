# AS-003P-R6 result

`AS003PR6_SOURCE_EVIDENCE_INSUFFICIENT_FOR_L2`

The source-preserving full-obligation L2 relation is coherent and passed `23/23`
pure tests twice identically. Retained R5A frames nevertheless omit the supported
worst-case route movement demand required by the AS-003L contract. They also do not
connect SelfModel progress/completion support to each opportunity. The analysis may
not substitute the modal point value `completion_ticks=0` or invent travel time.

Results:

- R5A frames read: `500`; exposed AS-003L conflicts: `57`.
- Candidate pairs audited: `7,457` total; `1,164` exposed-conflict pairs.
- Equal top-level class but different immediate successor physiology: `6,030` pairs.
- Previously admitted modal witnesses reclassified timing-UNKNOWN: `5,323`.
- Candidate branches: `5,367 SCHEDULE_UNKNOWN`; complete MUST/MAY schedules `0`;
  proven no-schedule branches `0`; one-way L2 distinctions `0`.
- AS-002 disposition: `NO_RELATION_SUPPORTED`.
- Production and existing-test semantic changes: `0`.
- Organism/control/shadow/diagnostic executions and retries/reseeds: all `0`.

The exact missing source fields are a per-opportunity supported worst-case route
movement demand (or equivalent route timing envelope), its source-semantic binding
to capability progress/completion, an explicit terminal-action timing convention,
and source-backed INSPECT route/opportunity evidence when stimulation is active.
No successor is authorized or started.
