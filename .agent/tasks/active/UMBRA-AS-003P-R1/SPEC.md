# UMBRA-AS-003P-R1

## Authority

Fresh protocol recovery from exact baseline
`6bc2f6633e356602a6ffe2338eef9b98bdac6e0c` after Architect acceptance of
terminal parent verdict `AS003P_EVIDENCE_INSUFFICIENT`.

## Objective

Byte-preserve the AS-003P scientific implementation, attribute the original
zero-tick import failure, prove repository-root module importability without
constructing an organism, freeze one exact command, and acquire exactly one
matched control plus one shadow execution of R0/S0 seed `45878900`, horizon
`500`.

## Scope locks

- Scientific AS-003P files, fixture, seed, horizon, parity semantics, and
  stochastic behavior are immutable.
- The original AS-003P harness and evidence remain terminal and unchanged.
- Fresh changes are limited to protocol harness/bootstrap, preflight tooling,
  governance records, and the fresh evidence root.
- After command lock: no code, command, environment, fixture, or repair change.
- Retries `0`; reseeds `0`; no behavioral authority or successor start.

## Terminal boundaries

Stop on start-state mismatch, preflight organism creation/ticks, importability
failure, frozen execution failure, observer/RNG parity failure, incoherent
source-frame capture, or insufficient actual modal evidence.
