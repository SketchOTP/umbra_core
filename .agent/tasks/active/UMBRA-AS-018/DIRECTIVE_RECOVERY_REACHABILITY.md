# UMBRA-AS-018 — Recovery Reachability Envelope

Authority: PRE-LOCK DEVELOPMENT. AS-017 is terminal as
`AS017_FRESH_R1_FAIL`; no AS-017 formal seed may be replayed.

Implement the smallest policy-visible, body-independent Recovery Reachability
Envelope that preserves a bounded recovery opportunity before physiological
reserve is exhausted. It distinguishes `ROBUST_NOW`,
`BOUNDED_RECOVERY_OPPORTUNITY`, `MAY_ROUTE`, and `UNKNOWN_ROUTE`, accounts for
all four physiological dimensions, action cost, drift, body/self-model
support, uncertainty, and conservative bounded progress, and remains pure.

Integrate it only as a pre-critical constraint in the existing arbitration
path. Ordinary arbitration remains the preference authority. No forced
CHARGE/REST, authored need priority, scalar utility, planner rewrite, hidden
Habitat truth, threshold change, formal seed, formal lock, or AS-017 replay is
authorized. Required work includes source-bound forensic analysis without
replay, adversarial focused controls, protected/applicable regression, one
fresh disjoint 32-case excluded-development population on the final published
candidate, governance/evidence publication, and a readiness packet for later
Architect lock review.
