# UMBRA-AS-018 — Recovery Reachability Envelope

## Authority

PRE-LOCK DEVELOPMENT. AS-017 is terminal as `AS017_FRESH_R1_FAIL`; no AS-017
formal seed may be replayed. This task is a successor repair generation and
must not consume formal seeds or establish a scientific lock.

## Objective

Add the smallest policy-visible, body-independent recovery-reachability
constraint that preserves a bounded recovery opportunity before physiological
reserve is exhausted. Preserve ordinary arbitration, body switching, verified
denial learning, branch safety, hidden-truth boundaries, and the exact owner
physiology transition semantics.

## Immutable evidence

- AS-017 P0 closeout and the consumed seed `26489381` remain permanent.
- The retained trace/database are read-only forensic inputs.
- V5/V8C and all predecessor development/formal evidence remain separately
  attributable.

## Terminal boundary

No formal execution, formal-seed consumption, lock, threshold relaxation, or
population evidence claim is permitted. A final handoff is qualified only when
the focused mechanism tests, full regression, fresh excluded-development
challenge, and publication/governance evidence all support E0–E5.
