# AS-018 execution plan

1. Preserve AS-017 terminal evidence and publish the source-bound failure map.
2. Add baseline-red contract tests for reachability, uncertainty, interacting
   physiology, exact transitions, and candidate-specific filtering.
3. Implement a pure Recovery Reachability Envelope using only policy-visible
   support and explicit state/context; integrate it as a dormant constraint
   before `NO_SAFE_ACTION`.
4. Validate the historical failure-class fixture without replaying the formal
   seed, then run focused tests and the protected regression surface.
5. Prepare and execute one fresh, disjoint 32-case excluded-development
   challenge only after the candidate is published and startup checks pass.
6. Publish one AS-018 readiness packet and return for Architect lock review;
   do not launch formal qualification.
