# UMBRA-D-014H3D specification

Baseline: f054f24af0d5847f3d4b96270184f72d09fdbf41

This is a fresh, non-production, replay-first selector generation. It must
prove both selector completeness and causal integration before any organism
outcome is interpreted as H3D selector science.

H3C remains historical. Its coder verdict is preserved, its baseline R0/R1
outcomes remain valid current-stack reproductions, and its selector artifact
and two-per-regime sealed manifest are retained as protocol-invalid H3C
artifacts. They must not be modified or reused as H3D holdouts.

Required H3D scope:
- audit H3C against the authorized architecture;
- identify the smallest experiment-only seam replacing only the final
  pre-Governance choice;
- capture legitimate proposal alternatives and source provenance;
- specify and implement bounded direct route-to-benefit semantics with the
  full physiology vector and UNKNOWN neutrality;
- freeze the selector, seam, and harness before organism outcomes;
- prove disabled-hook parity;
- prove sentinel candidate handoff through Governance, Embodiment, and
  VerifiedOutcome;
- prove real H3D selector divergence through the same lineage;
- seal fresh, non-overlapping 3xR1/R2/R3 development holdouts;
- run synthetic replay before the gated organism sequence;
- run H3D-enabled fixed R0, then known R1, then fresh holdouts only if
  predecessor gates pass.

Prohibited: production selector authority, hidden truth in policy, scalar
survival scoring, fixed need/source priority, planner/search expansion,
threshold/effect/identity/D-010/H3B changes, formal D-014, D-013/AX,
retries, reseeds, and Atlas storage migration or retired storage paths.

Permanent evidence:
/srv/ATLAS/100_ACTIVE/Projects/UMBRA-CORE/evidence/live-evidence/d014h3d-causal-integrated-selector-r1/

Stop on incomplete semantics, injection/parity failure, replay failure,
scientific gate failure, new predecessor regression, or any need to invent
policy-visible candidates or hidden-truth fields.
