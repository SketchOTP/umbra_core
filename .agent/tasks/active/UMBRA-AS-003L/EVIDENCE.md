# AS-003L evidence protocol

All AS-003L artifacts are atomically written to the Atlas evidence root with file fsync, atomic rename, directory fsync, SHA-256 readback, and a final manifest. Any helper uses literal case matrices and static source text only; it imports no UMBRA runtime and cannot construct an organism, tick physiology, execute Embodiment, mutate persistence, learn, or consume organism RNG.
