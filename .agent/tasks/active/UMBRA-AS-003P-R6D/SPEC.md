# R6D scope lock

R6D is zero-run relation research. It preserves the R6 `l2_precedes()` implementation,
the R6C MAY-only route semantics, and the R6B-R1 verified route evidence. It compares
a closed-world diagnostic interpretation with a sound open-world interpretation that
retains residual route UNKNOWN unless an explicit authoritative closure source exists.
No production, R6 L2 implementation, R6C frame, R6B-R1 route evidence, AS-003L, or
AS-002 change is permitted.
