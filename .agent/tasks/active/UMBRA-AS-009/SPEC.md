# UMBRA-AS-009 — R2/R3 Habitat-authority protocol recovery

Architect authority: `UMBRA-AS-009` from exact baseline
`f5e73ec4a3f5b677590d079d2bf2e506a699134e`.

AS-008 remains permanently terminal as `AS008_PROTOCOL_FAIL`. Its completed
R0/R1 rows are inherited; partial seed `16827204` is protocol-only and is not
reused. AS-009 may add only a fresh experiment namespace, evidence tooling, and
governance/docs. Production semantics, existing scientific tests, and prior
evidence remain immutable.

The stale D-014 R2 social perturbation must be migrated from legacy Embodiment
habitat mutation to the current HabitatEngine sole-writer path. Only after
preflight and seed-disjointness locks may fresh R2/R3 populations run. A
post-lock protocol or scientific failure is terminal; retries and reseeds are
zero.

Integrated viability, lifecycle, long-horizon, real-time soak, causal ablation,
and CLOSE-03 remain unqualified until every required gate passes.
