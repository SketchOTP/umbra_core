# UMBRA-AS-006 — Executable Weak-Continuation Loss and Integrated Viability

## Authority

- Exact baseline: `2bc042a7e1861b6c0beacca95a310d1d61ed0e5d`
- Parent terminal result: `AS005_PROTOCOL_FAIL`
- Fresh evidence root: `/srv/ATLAS/100_ACTIVE/Projects/UMBRA-CORE/evidence/live-evidence/umbra-as-006-executable-weak-continuation-integrated-viability-r1/`

## Scope

Implement and qualify source-backed MAY continuation loss and preventive recovery slack. Preserve AS-005 partial evidence as uninterpreted history. Keep planning bounded, candidate-neutral at the common root, non-scalar, and subordinate to ordinary action selection. No R7 revival, CLOSE-03, hidden truth, weighting, source/owner priority, retries, or reseeds.

## Required boundaries

- `MAY` remains possible evidence and is never promoted to `MUST`.
- A known option is `PRESERVED` only when every supported immediate branch retains at least one exact source-permitted continuation.
- It is `DESTROYED` only when every supported immediate branch categorically invalidates that exact known option.
- Mixed, missing, or uncertain evidence is `UNKNOWN`; `UNKNOWN` cannot eliminate.
- Recovery slack is binary feasibility only: owner boundary time minus exact witnessed demand. It is not a score, preference, rank, or priority.
- No future route demand is reconstructed from distance/progress arithmetic.
- Route learning remains verified-outcome-only and has no planning reader beyond the authorized bounded bridge.

## Scientific sequence

Before freeze: focused proofs, source activation, observer parity, and full applicable regression. After freeze: one Diagnostic A, one Diagnostic B, one known R1/S16, then the authorized fresh populations/lifecycle/boundedness/ablation gates only if preceding gates pass. Any post-freeze scientific failure is terminal for AS-006.
