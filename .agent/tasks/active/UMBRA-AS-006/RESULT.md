# AS-006 result

Status: ACTIVE — governance start only.

Exact baseline: `2bc042a7e1861b6c0beacca95a310d1d61ed0e5d`.

AS-005 remains permanently terminal as `AS005_PROTOCOL_FAIL`. Its partial scientific traces are preserved and are not qualification or tuning evidence.

## AS-006 pre-scientific freeze

Implementation commit: `9e6f92a99a397abec9e1ac245dcc5767ed6dd19f`.
The executable weak-continuation bridge adds exact lived-option identity,
candidate-caused `PRESERVED / DESTROYED / UNKNOWN` branch status, and
demand-derived preventive activation without scalar authority. Focused proofs
pass `17/17` twice. The applicable repository boundary is `1298 PASS / 2 SKIP /
13 inherited FAIL`, with `0` candidate-only failures. A fresh source-activation
run completed `500/500`, acquired `500` route frames and `262` nonempty option
rows. A fresh common-root observer preflight passed with one control and one
shadow branch at `500/500`, zero semantic differences, and exact RNG parity.

Scientific protocol lock is published at the AS-006 evidence root. The next
commit is the dedicated freeze commit; after it, no code, test, fixture, or
protocol repair is permitted. Scientific A/B/R1 execution has not yet begun.
