# AS-016 plan

1. Reconcile baseline and copy retained AS-015 R2 evidence read-only.
2. Attribute the failure window only to the level supported by retained data.
3. Implement and unit-test the three source-contract repairs plus the
   cross-need recovery-reserve invariant.
4. Run affected/protected and complete applicable regressions; classify any
   exact-baseline failures separately.
5. Re-run non-formal R0--R3 and lifecycle/persistence/CLI preflights using
   excluded development seeds only.
6. Freeze fresh disjoint formal seeds and exact production/configuration
   fingerprints. Publish/read back the scientific lock before any new formal
   organism exists.
7. Run the frozen fresh population, then only on success lifecycle, repeated
   compaction 100k, S3, matched causal controls, and final assessment.
