# UMBRA-AS-002 handoff

Status: TERMINAL

Verdict: `AS002_DISTRIBUTED_COMPETITION_CONTRACT_SUPPORTED`

The supported contract is `SUPPORTED_DOMINANCE_DISTRIBUTED_COMPETITION_V1`. Each evidence channel represents one coherent proposition and is comparable only within its own qualified key. Candidate A eliminates B only when every applicable proposition is supported for both, A is no worse in every channel, and A is strictly better in at least one. `UNKNOWN` blocks elimination rather than becoming zero, bad, or unsafe.

The simultaneous nondominated frontier preserves genuine conflict without summing, normalizing, voting, or ordering channels. If several candidates remain, existing CLOSE-02Z candidate-local stochasticity selects exactly one; canonical behavioral identity resolves an exact stochastic tie. Hard safety, recoverability admissibility, urgent recovery, Governance, Embodiment, VerifiedOutcome, and selected-only learning remain outside or downstream of ordinary competition.

Implementation boundary: replace ordinary non-active/noncritical scalar scoring, additive temporal/individuality/fallback modifiers, scalar sorting, and scalar hysteresis-band choice. Add pure fixed-size SelfModel/WorldModel candidate views, channel adapters, supported-dominance resolution, and per-channel trace output. Preserve candidate generation/deduplication, protected authority, urgent recovery, Z stochastic identity, and all execution/learning boundaries.

No production code changed and no organism ran. Recommendation only: `UMBRA-AS-003_DISTRIBUTED_COMPETITION_IMPLEMENTATION_CANDIDATE`; not authorized.
