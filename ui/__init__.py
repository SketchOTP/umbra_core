"""UMBRA reference-companion UI layer. Interfaces around the organism core
(design: bodies/avatars/UI are interfaces, never the organism itself).
`umbra_core` and `experiments` must never import this package."""

from __future__ import annotations
