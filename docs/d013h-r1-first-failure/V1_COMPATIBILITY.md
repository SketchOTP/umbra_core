# V1 compatibility

V1 publication remains unchanged: it does not require the V2 evaluator trace
or V2 read-only artifact. The evaluator initialization lifecycle is selected
only for `P0_RECOVERY_CONTRACT_V2`.
