# Final recommendation

`D013F_CONTRACT_V2_VALIDATED`

The future P0 recovery contract is ready for a separately authorized formal
baseline and run. This directive does not authorize that run. The next single
action is to obtain a new formal baseline under the V2 contract, with the
read-only validator and campaign identity parameters pre-registered.

Scientific integrity: production organism unchanged, historical evidence and
verdicts unchanged, thresholds unchanged, no formal run, and no formal tag.
