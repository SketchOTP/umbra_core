# UMBRA-D-012A2 verdict

```text
UMBRA_D012A_FORMAL_SCHEDULE_FROZEN
```

Gates A through M pass. The frozen 19-event schedule completed through five
distinct organism worker processes with four planned restarts and five
quiesced checkpoints. Constitutional identity and the authoritative event
chain remained continuous, raw durable perception payload count remained zero,
and the final process audit found no worker, socket, or campaign lock.

The sole full-suite failure is byte-for-byte identical to the preregistered
D-010 tick-inventory fingerprint at `3a8b0b2`; D-010 and prior qualified seals
remain unchanged.

UMBRA-D-012B is authorized to launch formal P0 through the six-active-hour
checkpoint only. P1 and P2 remain separately gated. No formal P0 was launched
under this directive.
