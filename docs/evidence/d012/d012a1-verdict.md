# UMBRA-D-012A1 verdict

```text
UMBRA_D012A1_SUPERVISION_FAIL
```

The disposable mechanism run completed the 19-event frozen order, four restart
points, and five checkpoints through real organism persistence and the governed
adapter, habitat, and body paths. It retained no raw perception payloads.

Qualification is refused. The harness hosted the organism in the supervisor's
OS process, so distinct process authority, organism database ownership,
process-start identity, crash recovery, cleanup, and separated bounded logging
were not proved. Checkpoint and refusal coverage also remains incomplete at the
boundaries recorded in the accompanying artifacts.

Gate J also fails: the full suite reports one D-010 tick-classification inventory
failure (665 passed, 2 skipped). D-012A1 changed neither the protected D-010
inventory nor `umbra_core/runtime.py`; the deferred D-010 artifacts were left
unchanged.

D-012 remains active. D-012A remains incomplete. D-012B and formal P0 remain
unauthorized and unlaunched.
