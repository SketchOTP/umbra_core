# D-011C qualification design

The production boundary remains unchanged: a synthetic adapter creates a derived-only envelope; the organism assigns admission at its current core tick; the membrane retains only bounded envelopes and IDs; accepted and rejected decisions are ledger events. Replay validates the real SQLite hash chain and reconstructs these bounded decisions from those events. Diagnostic C1-C8 controls run only in temporary experiment databases and never alter production configuration or schemas.
