# Equivalence Results

Subject: D-009 `C0/S10`, seed 7, H0, bounded budget 80, interruption after
tick 35. The existing reference route and isolated shadow route matched
exactly for ticks, continuity metric, object/zone counts, boundedness, and
terminal completion. The production loader's RNG reconstruction path was
verified; RNG is classified as expected-to-reconstruct because loader
initialization may consume deterministic draws. Deterministic differences:
none. Explained nondeterminism: none. Verdict: `PASS`.
