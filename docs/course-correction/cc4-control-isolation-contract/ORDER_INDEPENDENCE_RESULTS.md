# Order Independence Results

The real reference pair and shadow pair were each executed in forward order
(experimental then control) and reverse order (control then experimental),
using fresh disposable state. Both pair results were identical. Verdict:
`PASS`.
