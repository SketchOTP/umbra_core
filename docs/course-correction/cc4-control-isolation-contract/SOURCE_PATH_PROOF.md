# Source Path Proof

Runtime proof resolved the real D-009 job builder, reference runner, production
organism creation and tick path, HabitatEngine, Store, raw-row function, and
comparison function to their repository modules. The shadow harness uses those
symbols and separate real SQLite state; no mocks or synthetic metrics replace
the reference path.
