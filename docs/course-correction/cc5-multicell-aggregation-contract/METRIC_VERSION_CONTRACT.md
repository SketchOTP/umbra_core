# Metric version contract

Metric identity is `habitat_individuality_separation`, version `d009-normalized-rate-v1`; the definition fingerprint binds matrix, scenario suite, thresholds, cells, and metric semantics.
