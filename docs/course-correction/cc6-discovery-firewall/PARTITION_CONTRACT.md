# Partition contract

Discovery, development-evaluation, and validation-embargo partitions are disjoint. Every synthetic sample ID belongs to exactly one partition. The embargo fixture is never exposed through the discovery reader.
