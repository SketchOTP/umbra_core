# Protected paths

The actual repository evidence root is `docs/evidence/`; production authority is `umbra_core/`; experiment definitions and formal runs are under `experiments/`. The firewall treats historical evidence, formal qualification, identity, governance, and production persistence as protected zones. Writes, absolute paths, traversal, and validation-path escapes fail closed.
