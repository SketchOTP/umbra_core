# Fault injection results

26/26 required faults A–Z were detected and rejected with zero silent failures: embargo reads, protected writes, forbidden/unknown/out-of-range/type variables, frozen-contract mutations, fingerprint/provenance errors, direct promotion, quarantine bypass, finalized-record edit, seed reuse, adaptive validation, and traversal/symlink escape.
