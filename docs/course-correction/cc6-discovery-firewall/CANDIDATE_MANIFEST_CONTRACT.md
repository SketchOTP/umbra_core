# Candidate manifest

Each manually declared candidate carries an ID, configuration/fingerprint, allowlist schema, evaluator and partition fingerprints, source commit, timestamp/provenance, diagnostic score, rank, and status. Initial and final status is `QUARANTINED`.
