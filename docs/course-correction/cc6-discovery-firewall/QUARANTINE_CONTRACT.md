# Quarantine

Discovery output writes only to research-owned quarantine. Allowed lifecycle labels are `QUARANTINED`, `REJECTED`, and `SELECTED_FOR_FUTURE_FREEZE`; no automatic transition reaches `VALIDATED`, `QUALIFIED`, `PRODUCTION`, or `AUTHORIZED`.
