# Blind validation design

A future operator-controlled interface may accept a frozen candidate only after search closure and return one controlled result. It must not expose seed IDs, trajectories, scores, verdict labels, or adaptive feedback during search. CC-6 validates only synthetic fixtures and does not run formal validation.
