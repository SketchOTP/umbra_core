# Provenance

The chain is source fixture → sanitized input → partition → evaluator → candidate configuration → score → rank → quarantine. Every link is fingerprinted. Changes to source, partition, evaluator, configuration, or source commit reject the record.
