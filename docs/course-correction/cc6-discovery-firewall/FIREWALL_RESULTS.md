# Firewall results

PASS. Six zones are explicit; partitions are disjoint; embargo reads fail; protected writes and traversal fail; default-deny variables reject forbidden/unknown/range/type violations; evaluator and provenance fingerprints are enforced; three candidates rank deterministically and remain quarantined; protected and formal authority writes are unavailable.
