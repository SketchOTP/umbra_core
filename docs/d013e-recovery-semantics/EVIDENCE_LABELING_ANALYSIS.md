# Evidence labeling analysis

D-013D's outer dossier correctly identifies D-013D and its formal verdict.
The inherited `p0-run-result.json` retains historical internal labels
`UMBRA-D-012B` and `UMBRA_D012B_P0_INTEGRITY_FAIL`. This is an evidence-label
ambiguity, not a scientific result to be rewritten. D-013E records the
ambiguity and preserves the frozen file byte-for-byte.
