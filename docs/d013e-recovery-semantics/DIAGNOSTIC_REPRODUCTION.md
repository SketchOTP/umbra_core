# Diagnostic reproduction

A disposable real-runtime worker was used outside the formal evidence tree.
The diagnostic seeded the D-013D physiology, resource geometry, estimated
distance, confidence, uncertainty, and serialized `energy` recovery focus.
Instrumentation showed the arbitrator selecting `CHARGE` for the forced
observation. In the fresh worker, a later world-model proposal mediation
changed the executed capability to `APPROACH`; this is a diagnostic-state
difference, not a modification to D-013D evidence.

The authoritative D-013D frozen trace remains the exact record of the formal
terminal event. The diagnostic therefore establishes the causal path and
boundary behavior without being represented as a second formal run.
