# Corrective recovery

Scenario:

`SAFE_DENIAL` → verified `APPROACH` → material context change → retry →
authoritative CHARGE success with positive energy effect.

Result: `VERIFIED_RECOVERY_SUCCESS`.

The semantic hardening therefore does not make V2 artificially rigid.
