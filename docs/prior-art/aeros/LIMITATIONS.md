# Limitations

- aeros-core git history depth observed as 2 (shallow/new publish)
- Upstream bridge E2E not fully run (optional extras/hardware)
- 2 upstream ecm sandbox/seccomp failures recorded, not patched
- Paper metrics not re-run → PAPER_ONLY where no source test
- Independent harness is contract proof, not production crypto HSM
- Apache reuse still requires operator legal review before copying files
