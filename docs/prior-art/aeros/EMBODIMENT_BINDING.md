# Embodiment binding

Distinguish: identity ≠ embodiment binding ≠ body model ≠ sensor/actuator roster ≠ capability roster.

Default: **one active primary embodiment** per individual.

Body replacement preserves `agent_id`. Incompatible skills become **DORMANT** (not forgotten). Old body cannot resume after migration without new transition. Duplicate primary binding rejected.
