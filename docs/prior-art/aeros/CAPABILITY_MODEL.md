# Capability model

Capability = executable competence, **not** identity.

Manifest: id, version, interface_version, hashes, signature, permissions, allowed_effects, sensors/actuators, limits, pre/postconditions, rollback contract.

ECM packaging: **REFERENCE**. Prefer simpler companion capability contract over marketplace ECM terminology in the organism core.
