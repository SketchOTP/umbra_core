# Pathological repeat results

NON_FORMAL_TEST: safe denial followed by a materially identical denial with no corrective action, no new evidence, and recovery still blocked.

Result: PASS. The worker-side V2 adjudicator returned `RECOVERY_FAILED` without using an arbitrary retry count.
