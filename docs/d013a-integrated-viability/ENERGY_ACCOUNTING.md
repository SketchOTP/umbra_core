# Energy Accounting

At the authoritative B2 failure tick, energy was `0.0525` before the action. The selected `MOVE` outcome cost `0.005`; autonomous drift contributed `-0.002`; the resulting energy was `0.0485`, below the critical lower bound `0.05`. The next recorded failure state reached `0.0015` by tick 191.

The corrected bounded R0 run reached tick 191 with energy `0.2065`, without a critical state. This is diagnostic evidence, not a formal P0 qualification result.
